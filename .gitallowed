# Git secrets allowed patterns
# These are false positives for legitimate code patterns

# SQL transaction commands
BEGIN;
BEGIN\s

# Common SQL patterns that are not secrets
type:\s*'service_account'
